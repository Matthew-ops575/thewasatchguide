[build]
  command = "npm run build"
  publish = ".next"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- src/ content/ public/ package.json package-lock.json next.config.ts postcss.config.mjs tsconfig.json netlify.toml"

[[plugins]]
  package = "@netlify/plugin-nextjs"
